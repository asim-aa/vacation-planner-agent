LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=dummy-key
LLM_MODEL=openai/gpt-oss-20b
LLM_TIMEOUT_SECONDS=120

# RapidAPI key for the "Air Scraper" (sky-scrapper) API. No longer used
# by flight_tool.py as of v3 (switched to fast-flights, which needs no
# key) -- kept here for the still-pending Hotel Agent v2 swap, which was
# planned to reuse this same subscription. Free tier is capped at
# 20 requests/month. Get one at https://rapidapi.com/apiheya/api/sky-scrapper/pricing
RAPIDAPI_KEY=your_rapidapi_key_here
